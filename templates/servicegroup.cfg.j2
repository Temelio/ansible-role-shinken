define servicegroup {
    servicegroup_name   {{ item.servicegroup_name }}
    alias               {{ item.alias }}
{% if (item.members | count) != 0 %}
    members             {{ item.members | join(',') }}
{% endif %}
}

