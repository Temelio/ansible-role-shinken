---

# Role travis tests

- hosts: localhost
  remote_user: root
  roles:
    - "Temelio/ansible-role-shinken"
  post_tasks:
    - include: ./test_common.yml
  vars_files:
    - "{{ role_path }}/tests/test_vars.yml"

